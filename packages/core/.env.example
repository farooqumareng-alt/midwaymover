# Copy to .env and fill in — never commit the real .env.
# Generate a local dev value with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
MFA_ENCRYPTION_KEY="CHANGE_ME_32_RANDOM_BYTES_BASE64"
